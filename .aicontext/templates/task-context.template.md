# Task Context: [Version] — [Feature Name]

<!-- In-flight working knowledge that doesn't fit the spec contract. Append entries prefixed `[Step N]`, 1-2 lines each. Never delete existing entries. See `process.md → Task-context content boundary` for what belongs here vs in the spec. -->

## Codebase Patterns

## Gotchas

## Decision Overrides
<!-- Supersession log: spec decisions overturned mid-task (old value + why). Everything else lives in the spec. -->

## Notes
<!-- Freeform investigation notes, debug logs, longer analysis. Keeps task-context as the single source of task working knowledge. -->
