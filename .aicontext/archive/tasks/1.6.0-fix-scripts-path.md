# Task 1.6.0: Fix PR scripts path resolution

## Created:
April 2, 2026

## Objective:
Fix PR workflow scripts (`pr-reviews.js`, `pr-resolve.js`) to resolve output paths relative to `__dirname` instead of `cwd`. Currently, when the git root differs from the project root (e.g., SSP), running `cd /git-root && node /project/.aicontext/scripts/pr-reviews.js` creates output files in the git root instead of the project's `.aicontext/data/`.

## Plan:

### Step 1: Remove legacy `.claude/scripts/` directory
- [x] Update test imports from `.claude/scripts/` to `.aicontext/scripts/`
- [x] Delete `.claude/scripts/` directory
- [x] Update `structure.md` — moved scripts to `.aicontext/` tree, updated skill examples

### Step 2: Fix path resolution in scripts
- [x] `pr-reviews.js` — change `OUTPUT_DIR` to use `path.join(__dirname, '..', 'data', 'github-pr-reviews')`
- [x] `pr-resolve.js` — checked, no fix needed (takes file path as CLI argument)
- [x] Update tests if needed — no changes needed, 130/130 passing

## Completion Notes:

- Moved test imports from `.claude/scripts/` to `.aicontext/scripts/`, deleted legacy directory
- Fixed `pr-reviews.js` OUTPUT_DIR to use `__dirname`-based path — output goes to correct `.aicontext/data/` regardless of cwd
- `pr-resolve.js` unaffected — takes file path as CLI argument, no hardcoded output paths
- 130/130 tests passing

---

Last Updated: April 2, 2026
