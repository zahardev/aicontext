# Task 1.7.0: Config file, commit codepath, and question UX

## Created:
April 4, 2026

## Spec:
[spec-1.7.0-framework-improvements.md](../specs/spec-1.7.0-framework-improvements.md)

## Objective:
Extract settings from project.md prose into `.aicontext/config.yml`, unify commit logic via commit.md as single codepath, and add global UX rule for closed questions.

## Plan:

### Step 1: Create config.yml format and template
- [x] Design `.aicontext/config.yml` structure (commit rules, task naming, spec naming)
- [x] Design `config.local.yml` override mechanism (gitignored, merges on top of config.yml)
- [x] Create config template with defaults

### Step 2: CLI changes — init and update support for config.yml
- [x] Update `aicontext init` to create `config.yml` with defaults
- [x] Update `aicontext update` to merge new keys without overwriting user values
- [x] Add `config.local.yml` to `.aicontext/.gitignore`

### Step 3: Unify commit codepath
- [x] Update `commit.md` to read from `config.yml`, apply `co_authored_trailer` with override language
- [x] Refactor `finish-task.md` step 6 to delegate actual commit to `commit.md`
- [x] Update `step-loop.md` line 8 to delegate to `commit.md`
- [x] Update `run-steps.md` commit setup section to read from `config.yml`

### Step 4: Update all prompts and standards to use config.yml
- [x] Update `standards.md` Commit Style to reference `config.yml`
- [x] Update prompts that read commit settings: `do-it.md`, `run-step.md`, `gh-review-fix-loop.md`, `aic-help.md`
- [x] Update prompts that read task naming: `create-task.md`, `plan-tasks.md`, `start-feature.md`
- [x] Update `generate.md` to customize `config.yml` interactively during `/start`
- [x] Remove settings section from `project.md` (keep documentation-only content)
- [x] Add global question UX rule to `standards.md` (AskUserQuestion in Claude Code, numbered options elsewhere)

### Step 5: Documentation updates and config migration fallback
- [x] Remove project.md prose fallback from `commit.md` and `finish-task.md` — create config.yml from template instead
- [x] Add migrate-and-cleanup behavior when creating config.yml on existing projects
- [x] Update documentation: `README.md`, `docs/development-model.md`, `docs/skills.md`, `.aicontext/structure.md`, `.aicontext/readme.md`, `task.template.md`

### Step 6: Add claude.question_style setting
- [x] Add `claude.question_style` to config template and config.yml (interactive | numbered)
- [x] Update `standards.md` Question UX rule to read from config.yml

### Step 7: CLI interactive config setup during init/update
- [x] Add interactive questions to `aicontext init`: task naming, commit mode, update check frequency
- [x] Add interactive questions to `aicontext update` for new settings not yet in user's config.yml
- [x] Write answers to config.yml
- [x] AI fallback: if config.yml missing, create from template with defaults and tell user to edit

### Step 8: Tests for installConfig and interactive setup
- [x] Add tests for `installConfig`: init creates config, update creates config if missing, update merges missing sections when config exists, existing values preserved
- [x] Add tests for `setConfigValue`: set value, cross-section safety, uncomment commented key, missing section

### Step 9: Move base_branch from release.md to config.yml
- [x] Add `project.base_branch` to config.yml and config template
- [x] Update `release.md` to read from config
- [x] Update prompts that use base branch: `prepare-release.md`, `draft-pr.md`, `review-scope.md`

## Completion Notes:

- Built config.yml with 5 sections (commit, task_naming, spec_naming, update_check, claude), reusable ensure-config.md prompt, CLI interactive setup, and full prompt migration
- co_authored_trailer evolved during implementation: added `false` (disable) and `default` (AI built-in) modes beyond custom string
- finish_action gained `ask` option; removed unsupported `commit+push+pr` that was still listed from 1.6.0
- Discovered and fixed: generate.md item was checked off with only partial implementation — led to new "Checkbox Discipline" rule in process.md
- Moved base_branch from release.md to config.yml — prompts now use ensure-config.md instead of hardcoded 'main'
- CLI init auto-detects main/master/develop and offers numbered options
- Follow-up: process-prompts task (grill-me, brainstorm, thoughts, start-feature rewrite) and update-notifications task remain

---

Last Updated: April 5, 2026
