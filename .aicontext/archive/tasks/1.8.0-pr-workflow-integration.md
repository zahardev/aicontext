# Task 1.8.0: PR Workflow Integration

## Created:
April 11, 2026

## Spec:
[spec-1.8.0.md](../specs/spec-1.8.0.md)

## Objective:
Add PR config guard to prevent accidental GitHub PR creation, and integrate PR + review loop into finish-task as optional gates with resumable task close.

## Deliverables:
- [x] `pr` config section in `config.template.yml` and `config.yml` mirroring `issue`
- [x] `draft-pr.md` respects config — never creates GitHub PR when `create_in_github: false`
- [x] `finish-task.md` supports `after_task.pr` and `after_task.review_loop` gates
- [x] finish-task is resumable — deferred tasks close silently on second run
- [x] `run-task.md` ask-batching includes new `after_task` fields

## Plan:

### Step 1: PR config section
- [x] Add `pr` section to `config.template.yml` with `save_to_file: true` and `create_in_github: ask`
- [x] Add `after_task.pr: false` and `after_task.review_loop: false` to `after_task` in template
- [x] Add same entries to project's `config.yml`
- [x] Update `ensure-config.md` if needed for backfill of new keys

### Step 2: Rewrite draft-pr.md
- [x] Add config reading via `ensure-config.md`
- [x] Add "Save and Create" section mirroring `draft-issue.md` pattern (save_to_file gate, create_in_github gate with two-stage ask UX)
- [x] Explicit guard: when `create_in_github: false`, do not create GitHub PR

### Step 3: finish-task PR and review loop gates
- [x] Add PR gate after push step — delegates to `draft-pr.md` when `after_task.pr` resolves to yes
- [x] Add push dependency check — if PR needs push but push didn't happen, ask user
- [x] Add review loop gate — delegates to `gh-review-fix-loop.md` when `after_task.review_loop` resolves to yes and remote PR exists
- [x] Skip review loop silently when no remote PR

### Step 4: Resumable finish-task
- [x] Add defer/mark-done ask when review_loop enabled but couldn't run
- [x] Add resumed-run detection (completion notes filled + all steps checked)
- [x] Resumed run silently marks done in worklog + outputs summary
- [x] Full pipeline completion marks done automatically

### Step 5: run-task ask-batching
- [x] Add `after_task.pr` and `after_task.review_loop` to the upfront ask batch in `run-task.md`

## Completion Notes:

- Design evolved during implementation: "always ask before marking done" replaced the original auto-mark logic, covering manual PR/review workflows and incomplete external reviews
- Review finding: standalone `/finish-task` (not via `/run-task`) needed inline ask-batching for unresolved `ask` values — added to the prompt
- `start.md` also improved: explicit note that project.md contents are loaded from step 1 read

---

Last Updated: April 11, 2026
