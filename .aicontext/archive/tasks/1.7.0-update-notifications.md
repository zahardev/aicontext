# Task 1.7.0: AI-side update notifications

## Created:
April 4, 2026

## Spec:
[spec-1.7.0-framework-improvements.md](../specs/spec-1.7.0-framework-improvements.md)

## Objective:
Surface AIContext update notifications through the AI (via `/start`) so users who never run CLI commands still learn about updates.

## Plan:

### Step 1: Add lastChecked to cache
- [x] Write `lastChecked` date to cache file when network check occurs
- [x] Ensure `latestVersion` remains readable in cache regardless of TTL age

### Step 2: Update `/start` prompt
- [x] Add update check step to `start.md`
- [x] Read `update_check.frequency` from config and `lastChecked` from cache to gate the check
- [x] Add first-run frequency preference prompt
- [x] Show cached notification when update exists, even if not time to check
- [x] Add upgrade offer with accept/dismiss options
- [x] Add WebFetch fallback for non-CLI environments

## Completion Notes:

- Added `lastChecked` ISO date to cache file; `readCache()` now returns full object so `/start` can read stale data
- Update check logic in separate `update-check.md` prompt (not inline in `start.md`) to keep session focus on project readiness
- WebFetch fallback for non-CLI environments reads `.aicontext/.version` + npm registry
- Follow-up: `setConfigValue` regex injection is latent (F-01 from deep review) — fix in a future task
- Follow-up: `checkForUpdates()` TTL branching has no unit test — needs https mocking (F-04)
- Improved `review-plan.md` with behavioral alignment check (added to process-prompts task as Step 7)

---

Last Updated: April 5, 2026
