# Task 1.7.0: Draft Issue Improvements

## Created:
April 10, 2026

## Spec:
[spec-1.7.0-draft-issue-improvements.md](../specs/spec-1.7.0-draft-issue-improvements.md)

## Objective:
Extend `/draft-issue` to optionally create GitHub issues, extract the issue template, and fix quality problems (missing titles, implementation details in requirements).

## Deliverables:
- [x] `issue.save_to_file` and `issue.create_in_github` config parameters with ask/save flow
- [x] Issue body template in `.aicontext/templates/issue.template.md`
- [x] Prompt rewritten: uses template, anti-pattern guidance, GitHub creation flow
- [x] Config template updated with `issue:` section
- [x] No behavior regression for existing `/draft-issue` usage (local file still created by default)
- [x] `{issue_id}` placeholder restored in `task_naming.pattern` config documentation
- [x] `/draft-issue` auto-fills `{issue_id}` for subsequent task creation when issue was just created
- [x] CLI `aicontext init` uses `{issue_id}` placeholder instead of custom prefix

## Plan:

### Step 1: Issue template and config
- [x] Create `.aicontext/templates/issue.template.md` with Title, Summary, Requirements sections
- [x] Add `issue:` section to `config.template.yml` with `save_to_file: true` and `create_in_github: ask`
- [x] Add `issue:` section to project's `config.yml`

### Step 2: Rewrite draft-issue prompt
- [x] Rewrite `draft-issue.md` to reference the template, read config via `ensure-config.md`
- [x] Add anti-pattern example for implementation details in requirements
- [x] Add GitHub creation flow with three-step ask/save/follow-up UX

### Step 3: Restore {issue_id} in task naming
- [x] Add `{issue_id}` to `task_naming.pattern` documentation in config template comments
- [x] Update `create-task.md` to handle `{issue_id}` placeholder (ask user for issue number)

### Step 4: Auto-fill task_id from draft-issue
- [x] Update `draft-issue.md` to note the created issue number for downstream use
- [x] Update `create-task.md` to check conversation context for a recently created issue number before asking

### Step 5: Update CLI init to use {issue_id}
- [x] Replace custom prefix logic with `{issue_id}` placeholder in `aicontext init`
- [x] Remove the "Issue tracker prefix" follow-up question
- [x] Update issue ID option to set pattern `"{version}-{issue_id}-{task-name}"`

## Completion Notes:

- Scope grew from 2 steps to 5: `{issue_id}` regression fix, auto-fill, and CLI init alignment were discovered mid-task
- Drive-by: fixed review playbook mapping gap in `run-task.md` and `step-loop.md` (`partial` → `review.md`, `full` → `deep-review.md`)
- Drive-by: added `.aicontext/config.yml` to `.gitignore` — framework project's config shouldn't ship with npm package

---

Last Updated: April 10, 2026 (Step 5)
