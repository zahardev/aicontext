# Task 1.6.0: Configurable prepare-release skill

## Created:
April 1, 2026

## Spec:
[spec-1.6.0-professional-dev-flow.md](../specs/spec-1.6.0-professional-dev-flow.md)

## Objective:
Make the `prepare-release` skill project-agnostic by introducing `.aicontext/release.md` as a per-project release config. On first run, the skill discovers what it can and interviews the user for the rest, then generates the config from a template. Subsequent runs follow the config.

## Plan:

### Step 1: Create release.template.md
- [x] Create `.aicontext/templates/release.template.md` with YAML frontmatter (`version_source`, `base_branch`, `version_detection`, `changelog_format`, `changelog_source`), markdown files table (File | Update | Hint/Notes), and `## Notes` section
- [x] Use full relative paths in file table examples
- [x] Add `changelog_source` field — internal AI changelog read as primary input for public CHANGELOG.md
- [x] Add commented field explanations to guide users filling it out

### Step 2: Update prepare-release prompt
- [x] If `release.md` exists: read it and follow the files table instead of hardcoded file list
- [x] If `release.md` is missing: discover first (scan for version files, changelog, README patterns, git tags for version format), then ask only for what can't be determined (base branch, version detection strategy, whether to create CHANGELOG.md if absent)
- [x] After interview, generate `release.md` from template, show to user for review, then proceed with the release
- [x] Keep universal steps in prompt: determine version, read task files, git diff against configured base branch, present summary, wait for confirmation

### Step 3: Polish prompt and skills
- [x] Merge Step 3 "Write changelog entries" principles inline into Step 4 — eliminate phantom step, put rules where they're used
- [x] Scope `## Notes` changelog override to `### Changelog Style` subsection specifically
- [x] Update skill descriptions (Claude + Codex) — remove outdated "update version, changelog, README version history"

### Step 4: Run tests
- [x] Run full test suite to verify no regressions

## Completion Notes:

---

Last Updated: April 1, 2026
