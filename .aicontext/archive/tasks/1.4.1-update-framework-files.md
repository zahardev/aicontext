# Task 1.4.1: Update framework files distributed to users

## Created:
March 5, 2026

## Objective:
Ensure the `update` command distributes all recent framework improvements to user projects. Several files have been updated in the source but are not yet reaching users via `aicontext update`.

## Functional Requirements:
- Updated `readme.md` is distributed (prompt name fixes, new sections)
- `.gitignore` restructuring is distributed (ignore `local.md` instead of entire `data/`)
- New `data/.gitignore` is distributed
- New `examples/` directory is distributed
- Updated `.claude/CLAUDE.md` is distributed (new skill-invocation rule)

## Technical Considerations:
- Database changes needed? No
- API changes needed? No
- Breaking changes? No — `.gitignore` change shifts from ignoring `data/` to ignoring `local.md`, but user content in `data/` was already untracked so no data loss
- Third-party integrations affected? None
- Performance implications? None
- Security considerations? None

## Plan:

### Step 1: Ensure `update()` distributes `.aicontext/readme.md`
- [ ] Source already has correct content (prompt references, Data Directory section, PR workflow docs, agents/skills tables) — ensure `update()` copies it

### Step 2: Ensure `update()` distributes `.aicontext/.gitignore`
- [ ] Change from ignoring `data/` to ignoring `local.md` only

### Step 3: Ensure `update()` distributes `.aicontext/data/.gitignore`
- [ ] New file for selective gitignoring inside `data/`

### Step 4: Ensure `update()` distributes `.aicontext/examples/`
- [ ] New directory with 4 example project configs (cli-tool, laravel-api, web-api, wordpress-plugin)

### Step 5: Ensure `update()` distributes `.claude/CLAUDE.md`
- [ ] Add rule: "When a skill is invoked via `/command`, its content is provided inline — NEVER read files from `.claude/skills/` directories"

### Step 6: Tests and version bump
- [ ] Write/update tests for the update behavior
- [ ] Update version to 1.4.1
- [ ] Update changelog

## Bugs & Issues Encountered:

---

_Add more issues as encountered during implementation_

## Testing:
- [ ] Unit tests written and passing
- [ ] Edge cases tested
- [ ] Manual testing completed

## Completion Notes:

### Follow-up Tasks:

### Documentation Updated:
- [ ] `structure.md` updated if architecture changed
- [ ] `changelog.md` updated with this task

---

Last Updated: March 5, 2026
