# Task 1.8.0: Explicit review playbook paths and rename review vocabulary

## Created:
April 12, 2026

## Spec:
[spec-1.8.0.md](../specs/spec-1.8.0.md)

## Objective:
Make `step-loop.md` and `run-task.md` spell out the exact playbook file path for the reviewer, so neither the lead agent nor inline execution can pick the wrong criteria. Rename `partial`→`normal` and `full`→`deep` to match the playbook names.

## Deliverables:
- [x] `step-loop.md` and `run-task.md` specify exact playbook paths
- [x] Inline (Cursor/Copilot) path equally explicit
- [x] Config vocabulary renamed: `normal|deep|false|ask` (aliases `partial`/`full` accepted)
- [x] Migration in `ensure-config.md` rewrites `partial`→`normal`, `full`→`deep`
- [x] Config template uses new vocabulary
- [x] Drive-by: `after_task.tests` label "Step-related" → "Task-related"

## Plan:

### Step 1: Rename config vocabulary and add migration
- [x] Update `config.template.yml` comments: `partial|full` → `normal|deep` (aliases accepted)
- [x] Update this project's `config.yml` comments and values
- [x] Add deprecated-key mapping in `ensure-config.md`: `partial`→`normal`, `full`→`deep`
- [x] Update `step-loop.md` ask-UX tables to use `normal`/`deep`

### Step 2: Update review instructions with explicit playbook paths
- [x] `step-loop.md`: replace "tell the reviewer which playbook" with exact paths, accept both `normal`/`partial` and `deep`/`full`
- [x] `run-task.md`: same update
- [x] Make the inline (non-subagent) path equally explicit

### Step 3: Update process.md quality checks
- [x] Update any `partial|full` references in `process.md` to `normal|deep`

## Completion Notes:

- Deep review caught 3 missed vocabulary references in aic-help.md and docs/development-model.md
- 1.7.0 migration targets collapsed to map directly to new vocabulary — avoids double-migration
- Drive-by fix: "Step-related tests only" → "Task-related tests only" in after_task ask-UX label

---

Last Updated: April 12, 2026

