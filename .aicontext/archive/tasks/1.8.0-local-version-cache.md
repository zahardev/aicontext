# Task 1.8.0: Local Version Cache

## Created:
April 12, 2026

## Spec:
[spec-1.8.0.md](../specs/spec-1.8.0.md)

## Objective:
Move version check cache from `/tmp` to `.aicontext/data/version.json`, derive cache path from target dir (no `--cache` flag), and include all three versions in cache.

## Deliverables:
- [x] CLI `aicontext version [path]` writes cache to `<path>/.aicontext/data/version.json` when `.aicontext/` exists
- [x] `--cache` flag removed — cache path derived from target dir
- [x] Cache schema: `{ cliVersion, currentVersion, latestVersion, lastChecked, nextCheck }` with YYYY-MM-DD dates
- [x] `check-update.md` uses cache-only — no separate `.aicontext/.version` read
- [x] `check-update.md` offers correct command: `upgrade` (CLI behind) vs `update` (project behind) vs both
- [x] Agent runs `date` to get current date before comparing `nextCheck`

## Plan:

### Step 1: Add --cache flag to CLI
- [x] Parse `--cache <path>` argument in `aicontext version` command
- [x] Route cache read/write to specified path when flag is present
- [x] Default to `/tmp/aicontext-version-cache.json` when flag is absent

### Step 2: Rewrite check-update.md
- [x] Replace `/tmp` cache path with `.aicontext/data/version.json`
- [x] Add `date` command step for reliable current date
- [x] Use `nextCheck` field for freshness — no frequency lookup at read time
- [x] Pass `--cache .aicontext/data/version.json` when calling CLI
- [x] Update fallback path (no CLI) to write local file

### Step 3: Tests
- [x] Test CLI `--cache` flag with custom path
- [x] Test default behavior unchanged without flag

### Step 4: Rewrite checkVersion to write cache from target dir
- [x] Remove `--cache` flag parsing from CLI
- [x] `checkVersion(targetDir)` writes `.aicontext/data/version.json` only when explicit path provided AND `.aicontext/` exists
- [x] Fetch latest from npm, read `.aicontext/.version`, write full cache: `{ cliVersion, currentVersion, latestVersion, lastChecked }`
- [x] `lastChecked` in YYYY-MM-DD format
- [x] No cache written when no path argument (manual `aicontext version` stays print-only)

### Step 5: Update check-update.md prompt
- [x] Command changes to `aicontext version {project_root}` (agent substitutes actual path)
- [x] Step 5 reads all versions from cache — no separate `.aicontext/.version` read
- [x] Offer `upgrade` when `cliVersion < latestVersion`, `update` when `currentVersion < cliVersion`, both when applicable

### Step 6: Update tests
- [x] Test cache is written to `.aicontext/data/version.json` under target dir
- [x] Test cache schema has all five fields with correct formats
- [x] Test no cache written when no path argument
- [x] Test no cache written when `.aicontext/` doesn't exist at target

### Step 7: Generate version.json on init and update
- [x] Call `writeVersionCache` at the end of `init()` and `update()` so the cache exists from first install

## Completion Notes:

**Steps 1–3 (original):** Added `--cache` flag to CLI, rewrote `check-update.md` to use `.aicontext/data/version.json`.

**Reopened — Steps 4–6:** `--cache` flag never worked (`checkVersion` didn't use it, schema mismatch between CLI and prompt). Replaced with simpler approach: `aicontext version <path>` derives cache location from target dir, writes `{ cliVersion, currentVersion, latestVersion, lastChecked }` — agent adds `nextCheck`. Prompt now offers `upgrade` vs `update` based on two-version comparison. WebFetch fallback dropped — update checks require CLI in PATH. Dead `cachePath` params cleaned up from `readCache`/`writeCache`/`clearCache`. `checkVersion` reuses `/tmp` cache when fresh to avoid redundant npm fetches.

**Step 7:** `init()` and `update()` now seed `version.json` at install time so `/start` never hits the missing-cache path on first run. `latestVersion` is null (no npm fetch during install); filled on first `/start` check. Review fix: `||` → `??` in `writeVersionCache` to avoid coercing falsy strings.

---

Last Updated: April 12, 2026 (Step 7)

