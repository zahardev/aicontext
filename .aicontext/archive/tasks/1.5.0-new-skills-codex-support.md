# Task 1.5.0: New skills, Codex support, and script relocation

## Created:
March 21, 2026

## Objective:
Pull in battle-tested features from active projects (ServiBoard, TransLitera, Slovnyk, SSP) into the shared AIContext framework: new Claude skills, Codex tool support, generic PR template, standards-checker model upgrade, and tool-agnostic script location.

## Functional Requirements:
- Add `/standards-check` skill (delegates to standards-checker agent for branch diff)
- Add `/draft-issue` skill (drafts GitHub issues, saves to data/issue-drafts/)
- Add `/code-health` skill (codebase-wide scan for refactoring opportunities — generic, not language-specific)
- Add `pr.template.md` template (generic, without test section)
- Upgrade `standards-checker` agent model from `sonnet` to `opus`
- Add Codex support: `.codex/skills/` with Codex-adapted versions of all framework skills
- Move PR scripts from `.claude/scripts/` to `.aicontext/scripts/` (tool-agnostic)
- Update `bin/aicontext.js` to handle new skills, Codex, and script location
- Update `package.json` for distribution (files, keywords, description)
- Update `.aicontext/readme.md` with new skills, Codex section, scripts location

## Technical Considerations:
- Database changes needed? No
- API changes needed? No
- Breaking changes? No — old `.claude/scripts/` is kept for existing users, not deleted on update
- Third-party integrations affected? None
- Performance implications? None
- Security considerations? None

## Plan:

### Step 1: Add new Claude skills and PR template
- [ ] Create `/standards-check` skill (from projects — identical across all 3)
- [ ] Create `/draft-issue` skill (from projects — identical across all 3)
- [ ] Create `/code-health` skill (based on TransLitera, made generic — remove hardcoded paths, make scope dynamic via structure.md)
- [ ] Create `pr.template.md` template (based on TransLitera, remove test section and project-specific commands)

### Step 2: Upgrade standards-checker agent
- [ ] Change `standards-checker.md` model from `sonnet` to `opus`

### Step 3: Move PR scripts to tool-agnostic location
- [ ] Copy `pr-reviews.js` and `pr-resolve.js` from `.claude/scripts/` to `.aicontext/scripts/`
- [ ] Update all skill references (pr-review-check, etc.) to use `.aicontext/scripts/` path

### Step 4: Add Codex support
- [ ] Create `.codex/skills/` with Codex-adapted versions of all framework skills (based on SSP pattern: self-contained workflows, no subagent delegation, no `disable-model-invocation`)

### Step 5: Update CLI (bin/aicontext.js)
- [ ] Add new skills to `FRAMEWORK_SKILLS` array
- [ ] Add `FRAMEWORK_CODEX_SKILLS` array
- [ ] Add `copyFrameworkCodexSkills()` function
- [ ] Update `copyFrameworkScripts()` to use `.aicontext/scripts/` path
- [ ] Add `.codex/` copy to `init()` and `update()` flows
- [ ] Add `.aicontext/scripts/` copy to `init()` and `update()` flows
- [ ] Update init/update display messages to mention Codex and scripts

### Step 6: Update package.json and readme
- [ ] Add `.codex/` and `.aicontext/scripts/` to `files` array
- [ ] Add `codex` to keywords, update description
- [ ] Update `.aicontext/readme.md` with new skills, Codex info, scripts location

### Step 7: Tests
- [ ] Update tests for new skills, Codex support, and script relocation
- [ ] Verify all tests pass

## Bugs & Issues Encountered:

_None yet_

## Testing:
- [ ] Unit tests written and passing
- [ ] Edge cases tested
- [ ] Manual testing completed

## Completion Notes:

### Follow-up Tasks:
- Update existing projects (ServiBoard, TransLitera, Slovnyk, SSP, Castos) via `aicontext update`

### Documentation Updated:
- [ ] `structure.md` updated if architecture changed
- [ ] `changelog.md` updated with this task

---

Last Updated: March 21, 2026
