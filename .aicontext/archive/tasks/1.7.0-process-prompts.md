# Task 1.7.0: Process prompts — grill-me, brainstorm, thoughts, review-plan, start-feature

## Created:
April 4, 2026

## Spec:
[spec-1.7.0-framework-improvements.md](../specs/spec-1.7.0-framework-improvements.md)

## Objective:
Create `/grill-me`, `/brainstorm`, and `/thoughts` skills, improve `/review-plan` with spec alignment, then rewrite `start-feature` to use grill-me as the discovery engine with mandatory spec+task creation.

## Requirements:
<!-- Backfilled retroactively per 1.7.0-spec-traceability Step 6. Concrete deliverables for this task — the spec subsections that this task implements (Interview and thinking skills, create-task spec handling, add-step task identification, aic-help quick-start ordering, start-feature file-creation enforcement, start-feature question atomicity, Interview breadth and decision capture, Question Pacing). -->
- [x] `/grill-me`, `/brainstorm`, `/thoughts` skills created with Claude Code, Codex, and prompt files
- [x] `/review-plan` and `/finish-task` check spec alignment (behavioral + coverage)
- [x] `start-feature` rewritten to delegate interview to `/grill-me` with prior-context guard, scope question, and mandatory spec+task output
- [x] `/run-steps` renamed to `/run-task` across SKILL.md, prompt files, and all references
- [x] `/create-task` asks the user about spec handling with 3 options (use existing / create new / standalone) and never decides autonomously
- [x] `/add-step here` keyword targets IDE-opened task; conflict-resolution fit check when IDE ≠ conversation task
- [x] `aic-help.md` reordered to lead with first-session command flow before conceptual material
- [x] `/start-feature` Step 7 creates spec + task(s) + brief(s) + worklog entries with mandatory output summary
- [x] `/start-feature` Step 5 (Commit Preference) shows 5 defaults, asks defaults-vs-override gate, then 4 atomic questions on override
- [x] `grill-me` plans dimensions before asking and walks them breadth-first with depth cap on surprises
- [x] `grill-me` closing summary is structured (Dimensions / Decisions / Open items / Out of scope) for verbatim caller copy
- [x] `start-feature` Step 7 copies grill-me's structured summary verbatim into spec Decisions and decision tree into brief Decisions
- [x] Question Pacing rule added to `standards.md` with atomic-default + batchable-exception structure; per-prompt reminders added to applicable prompts

## Plan:

### Step 1: Create grill-me skill
- [x] Create `/grill-me` skill with: one question at a time, recommended answers, codebase exploration before asking, explicit "No questions" when done, use AskUserQuestion for closed questions per global UX rule
- [x] Credit mattpocock's original in the skill

### Step 2: Create brainstorm skill
- [x] Create `/brainstorm` skill

### Step 3: Create thoughts skill
- [x] Create `/thoughts` skill — lightweight "what are your thoughts?" prompt for quick thoughts, concerns, or better ideas about the current discussion. Distinct from `/challenge` (heavy audit) — this flows with the conversation

### Step 4: Add spec alignment checks
- [x] Add spec alignment check to `review-plan.md` (verify all spec requirements have corresponding plan steps, and no plan steps are outside spec scope)
- [x] Add spec alignment check to `finish-task.md` (verify all spec requirements were actually delivered)

### Step 5: Rewrite start-feature interview flow
- [x] Replace inline product/engineering dimension interview with reference to `grill-me`
- [x] Add prior-context guard: summarize what's known, then run interview to fill gaps
- [x] Add scope question after interview ("limited to this discussion, or broader?")
- [x] Make spec + task creation mandatory (no direct implementation path)

### Step 6: Rename run-steps to run-task
- [x] Rename `/run-steps` skill to `/run-task` (SKILL.md, prompt file, all references in other prompts, and documentation including README)

### Step 7: Improve review-plan behavioral alignment
- [x] Add behavioral alignment as primary check (trace runtime behavior before comparing text)
- [x] Rename "Spec alignment" to "Spec coverage" with stricter wording ("fully addressed", not just "mentioned")

### Step 8: Improve create-task spec handling
- [x] Replace autonomous spec decision in Step 2 with an explicit 3-option question: (1) use existing spec, (2) create new spec from discussion, (3) standalone task
- [x] Option 2: create spec inline from discussion (lightweight — no interview, no grill-me), using problem/solution/requirements/decisions sections
- [x] Respect `claude.question_style` config for the question (interactive vs numbered)

### Step 9: Fix add-step task identification logic
- [x] Add `here` keyword support: `/add-step here` always targets the IDE-opened task file, no conflict check
- [x] Add conflict-resolution logic: if IDE task ≠ conversation task and no `here` keyword, check which task the step fits better based on objective; if equal fit, prefer IDE-opened
- [x] Handle edge case: `here` with no IDE task file open → fall back to normal logic

### Step 10: Reorder aic-help content for faster quick-start
- [x] Lift the first-session command flow (`/start → /start-feature → /run-task → /finish-task`) into its own section near the top, before "Key Concepts"
- [x] Promote "run `/start` at the beginning of every session" from parenthetical to a prominent rule
- [x] Move "Key Concepts" (Spec/Task/Brief/Worklog) after the first-session workflow so vocabulary anchors to commands the user has already seen
- [x] Consolidate the "Running Skills" mini-section into "After Init" / workflows intro and remove the duplicate `/aic-skills` mention
- [x] Replace the "After Init" folder inventory with a concrete next-step action

### Step 11: Harden `/start-feature` file-creation enforcement
- [x] Update step 1 (Context Gathering) to track codebase patterns discovered during exploration — captured in a running internal list that step 7 uses to seed the brief's Codebase Patterns section
- [x] Add post-interview handoff line between step 6 and step 7: *"Interview complete. Now creating spec, task(s), and brief(s)."* — resets agent attention after the long discussion phase
- [x] Update step 7 to also create a brief file per task from `.aicontext/templates/brief.template.md`, seeded with References (spec, task, rules, project, structure, local) + the Codebase Patterns captured in step 1 + the decision tree from Step 14 (already specified in Step 14's contribution to step 7)
- [x] Update step 7 to also append the spec + task entries to `.aicontext/worklog.md` under the `## In Progress` section, creating a new `### [Spec Name](specs/spec-{name}.md)` heading if one doesn't exist (matches the existing worklog pattern)
- [x] Add a mandatory output summary at the end of step 7 (similar to `close-step.md`), with "You MUST output this summary — it is the deliverable that proves files were created" framing. Summary must include explicit counts (e.g., "Created: 1 spec, 2 tasks, 2 briefs, 3 worklog entries") in addition to file paths — counts force the agent to verify what it actually created instead of asserting completion

### Step 12: Enforce atomic questions in `/start-feature`
- [x] Rewrite step 5 (Commit Preference): show all 5 commit defaults from config.yml (mode, template, body, finish_action, co_authored_trailer), ask a single defaults-vs-override gate, then if override — ask 4 atomic questions (mode → template → body → finish_action) one at a time with the project default as the recommended answer. `co_authored_trailer` is shown in defaults but not asked (edited in config.yml directly).
- [x] Flatten the nested sub-option lists (commit template, finish action) into inline prose (`manual / per-step / per-task`) so the format doesn't invite numbered batch rendering
- [x] Add a one-line reference at the top of step 5 pointing to the global Question Pacing rule in `standards.md`
- [x] Add a one-line reference to step 4 (scope question) noting it must be asked standalone — follow Question Pacing rule
- [x] Update the `start-feature question atomicity` spec requirement to reference the global Question Pacing rule and the expanded 4-question scope

### Step 13: Global Question Pacing rule (standards.md + per-prompt reminders)
- [x] Add new `### Question Pacing` subsection to `standards.md` under `## AI Response & Behavior Rules`, placed at the top of that section (above `Communication Style`). Action-gate wording: *"Before asking the user an open question, STOP. Ask only one. Wait for the answer before the next."* Include **Why:** (rules buried in long files get skimmed; per-question pacing forces attention) and a pointer to `claude.question_style` for closed questions.
- [x] Add a one-line reference to `check-task.md` Surface section: *"For any open questions, follow the Question Pacing rule in standards.md."*
- [x] Audit other prompts that surface open questions and add the same one-liner where applicable (candidates: `create-task.md`, `finish-task.md`, `add-step.md`, `review-plan.md`, `start-feature.md`)
- [x] Add `Question Pacing` requirement to the spec's Requirements section

### Step 14: Breadth-first interview with dimension map and structured summary
- [x] Add a "Plan dimensions before asking, revise as you learn" rule to `grill-me.md`: after context gathering, the AI builds an initial list of dimensions to cover (product scope, users, data shape, integration points, edge cases, non-goals, etc.) and walks them breadth-first. After each answer, check whether it reveals a new dimension and add it to the list — the dimension map is live, not fixed
- [x] Add a depth cap to `grill-me.md`: when an answer reveals something unexpected, ask at most 1–2 follow-ups, then explicitly return to the next unasked dimension
- [x] Add a "Decisions so far" running-list rule to `grill-me.md`: the AI maintains an internal list of decisions made during the interview and references it when asking follow-ups, to prevent mid-interview memory loss
- [x] Restructure grill-me's "No more questions" closing: replace the freeform summary with a structured block (Dimensions covered / Decisions / Open items / Out of scope) so the caller can copy it verbatim
- [x] Update grill-me's "one question at a time" rule to reference the global Question Pacing rule (atomic default + batchable exception for narrow follow-ups)
- [x] Add a standalone-mode next-action prompt to grill-me: when grill-me runs outside start-feature, end with "Want me to create a spec, a task, or leave it here?"
- [x] Update `start-feature.md` Step 1 (Context Gathering) to seed the dimension list from codebase exploration before handing off to grill-me
- [x] Update `start-feature.md` Step 7 to copy grill-me's structured summary verbatim into the new spec's Decisions section before paraphrasing anything
- [x] Update `start-feature.md` Step 7 to also copy the full decision tree (dimension map + all decisions) into the brief's Decisions section — preserves the *why* behind spec decisions for future reference
- [x] Add corresponding requirement to the spec under the existing `### Interview breadth and decision capture` section

## Completion Notes:

- Created 3 new skills (`/grill-me`, `/brainstorm`, `/thoughts`) with Claude, Codex, and prompt files
- Rewrote `/start-feature` to delegate interview to `/grill-me` with prior-context guard and mandatory spec+task output
- Added spec alignment checks to `/review-plan` (plan-time) and `/finish-task` (completion-time)
- Renamed `/run-steps` → `/run-task` with deprecated list entries for clean upgrades
- Deep review caught missing CLI arrays, Codex mirrors, and deprecated entries — fixed before closing
- Follow-up: version bump to 1.7.0 (user decided to defer)
- Rewrote `create-task.md` Step 2: replaced autonomous spec decision with explicit 3-option question (use existing / create new / standalone)
- Fixed `add-step.md` Step 1: `here` keyword targets IDE-opened task; conflict-resolution fit check when IDE ≠ conversation task

---

Last Updated: April 7, 2026 (Step 11 closed — all steps complete)
