Check this project. Confirm readiness in one sentence.

After confirming, add a hint about `/aic-help` (quick start guide) and `/aic-skills` (all available commands). Use `/command` syntax for Claude Code, use `use command` syntax for other tools.

After output, follow `update-check.md`.
