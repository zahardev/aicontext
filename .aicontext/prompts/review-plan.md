Review the current plan for:

1. **Spec alignment** - If the task links to a spec, verify:
   - Every spec requirement has at least one corresponding plan step
   - No plan steps are outside the spec's scope (unless the task explicitly extends it)
   - Flag uncovered requirements and out-of-scope steps separately
2. **Missing steps** - Any gaps in the implementation flow?
3. **Dependency issues** - Does any step depend on a later step?
4. **Over-engineering** - Unnecessary complexity or abstractions?

If no issues: confirm "Plan is valid" and summarize the implementation order.
